<?php

return [

    /*
    |--------------------------------------------------------------------------
    | MQTT Broker
    |--------------------------------------------------------------------------
    */

    'host' => env(
        'MQTT_HOST',
        '127.0.0.1'
    ),

    'port' => (int) env(
        'MQTT_PORT',
        1883
    ),

    /*
    |--------------------------------------------------------------------------
    | Authentication
    |--------------------------------------------------------------------------
    */

    'username' => env(
        'MQTT_USERNAME'
    ),

    'password' => env(
        'MQTT_PASSWORD'
    ),

    /*
    |--------------------------------------------------------------------------
    | Client
    |--------------------------------------------------------------------------
    */

    'client_id' => env(
        'MQTT_CLIENT_ID',
        'laravel-gps-tracker'
    ),

    'clean_session' => env(
        'MQTT_CLEAN_SESSION',
        true
    ),

    /*
    |--------------------------------------------------------------------------
    | Connection
    |--------------------------------------------------------------------------
    */

    'keep_alive' => (int) env(
        'MQTT_KEEP_ALIVE',
        60
    ),

    'timeout' => (int) env(
        'MQTT_TIMEOUT',
        5
    ),

    'qos' => (int) env(
        'MQTT_QOS',
        1
    ),

    /*
    |--------------------------------------------------------------------------
    | Topics
    |--------------------------------------------------------------------------
    */

    'topics' => [

        'gps' => env(
            'MQTT_TOPIC',
            'gps/+/location'
        ),

    ],

    'tls' => [

        'enabled' => (bool) env(
            'MQTT_USE_TLS',
            false
        ),

        'ca_file' => env(
            'MQTT_CA_FILE'
        ),

        'client_certificate' => env(
            'MQTT_CLIENT_CERT'
        ),

        'client_key' => env(
            'MQTT_CLIENT_KEY'
        ),

    ],

];
